note: readme тоже сделан с помощью иишки, кроме партов с моими промптами и своим видением проблем и ошибок, которые возникали, там иишка как-то неточно указала, как я делал и что думал, поэтому я отредачил, а остальное вроде бы иишка хорошо описала, поэтому оставил как есть. моё описание там ближе к концу есть
 
 # GB Empire Dashboard

Мини-дашборд заказов для тестового задания AI Tools Specialist.

Цепочка здесь такая:

`RetailCRM -> Supabase -> Next.js Dashboard -> Telegram`

## Deployment

Рабочий деплой на Vercel:

- [dashboard-nine-murex-68.vercel.app](https://dashboard-nine-murex-68.vercel.app/)

Это основной URL для проверки проекта.

## Что есть в проекте

- dashboard с заказами из Supabase
- сводные карточки и графики
- загрузка `mock_orders.json` в pipeline
- ручное добавление нового заказа через dashboard
- синхронизация заказов из RetailCRM в Supabase
- Telegram-уведомления для новых крупных заказов
- отдельная страница с отчётом: `/report`

## Важно перед проверкой

- Сейчас в таблице `orders` и в самом RetailCRM специально `0` заказов. Я оставил всё в чистом состоянии, чтобы сценарий проверки можно было пройти с нуля.
- Пример входных данных лежит в репозитории: `mock_orders.json`.
- Telegram-бот: `@GBEmpire_RetailCRM_bot`.
- Перед проверкой уведомлений нужно обязательно написать боту `/start`.

Без `/start` чат не попадёт в `telegram_users`, и отправлять уведомления будет просто некуда.

## Как этим пользоваться

### 1. Открыть dashboard

На главной странице есть:

- сводка по заказам
- графики по городам, источникам и статусам
- таблица заказов
- блок загрузки JSON
- форма ручного добавления заказа

Источник данных для dashboard: Supabase.

### 2. Загрузить mock JSON

На главной странице есть блок выгрузки заказов.

Что нужно сделать:

1. Ввести `pipeline secret`.
2. Выбрать `mock_orders.json` или свой JSON такого же формата.
3. Нажать `Запустить pipeline`.

Что делает pipeline:

1. принимает JSON
2. убирает дубли внутри входного файла
3. не загружает в RetailCRM заказы, которые там уже есть
4. загружает новые заказы в RetailCRM
5. забирает актуальные заказы из RetailCRM
6. синхронизирует их в Supabase через `upsert`

### 3. Добавить новый заказ вручную

На той же странице есть форма ручного добавления.

Через неё можно:

- заполнить контактные данные
- указать город и адрес доставки
- добавить один или несколько товаров
- отправить заказ в тот же pipeline

То есть ручной ввод и JSON-выгрузка идут по одной и той же логике, без второй отдельной серверной ветки.

### 4. Открыть report page

Есть отдельная страница:

- `/report`

Там я коротко показал, как собиралось приложение, где были проблемы и как я их раскладывал по шагам.

## Формат входных данных

Ориентир — `mock_orders.json`.

Pipeline принимает:

- либо JSON-массив заказов
- либо объект вида `{ "orders": [...] }`

Для ручной формы этот формат собирается автоматически.

## Telegram-уведомления

Уведомления приходят не на каждый заказ.

Текущая логика такая:

- уведомление отправляется только на **новый** заказ
- сумма должна быть **строго больше 50000 KZT**
- `50000` ровно не отправляется
- перед этим пользователь должен написать `/start` боту `@GBEmpire_RetailCRM_bot`

Ожидаемая цепочка:

`New order -> Supabase INSERT -> webhook -> /api/notifications/retailcrm-order -> Telegram`

## Безопасность

В проекте есть базовые меры защиты:

- Basic Auth на dashboard
- secret-проверка для pipeline route
- secret-проверка для webhook уведомлений
- server-only доступ к Supabase через `SUPABASE_SERVICE_ROLE_KEY`
- дополнительная защита Telegram webhook через secret token

Секреты и env-переменные сюда не дублирую. Файл с env передаётся отдельно.

## Локальный запуск

Что понадобится:

- Node.js
- npm
- доступ к Supabase
- доступ к RetailCRM
- Telegram bot token
- env-переменные

Установка:

```bash
npm install
```

Локальный запуск:

```bash
npm run dev
```

Проверка сборки:

```bash
npm run lint
npm run build
```

## Как проверить проект

Минимальный сценарий проверки такой:

1. Открыть dashboard: [dashboard-nine-murex-68.vercel.app](https://dashboard-nine-murex-68.vercel.app/)
2. Написать `/start` боту `@GBEmpire_RetailCRM_bot`
3. Загрузить `mock_orders.json` через pipeline
4. Убедиться, что заказы появились в dashboard
5. Добавить один заказ вручную через форму
6. Проверить, что синхронизация с RetailCRM и Supabase отработала
7. Проверить Telegram-уведомление на заказ с суммой выше порога
8. Открыть `/report`

## AI workflow

Эту задачу я собирал не одним большим запросом, а короткими итерациями. Сначала разбирался с данными и интеграциями, потом собрал dashboard, потом уже доводил pipeline, уведомления, защиту и подачу.

// HUMAN PART, так сказать

### какие промпты я давал примерно (использовал Codex)

- посмотри на тз и спланируй что нужно сделать: {текст тз тут}
- напиши Python скрипт который будет выгружать mock_orders.json в retailcrm через апишку
- теперь можешь сделать так чтоб с апишки retailcrm доставались заказы и выгружались в Supabase? если что напиши какие апи ключи и секреты тебе нужны, я потом добавлю
- оке кайф, можешь теперь сделать так чтоб в обоих скриптах учитывались дубликаты и чтоб они не создавались
- можешь написать dashboard на next.js и потом зааплодить на Vercel
- UI мне не нравится, вот тебе я на Google Stitch накидал, можешь его использовать как референс?
- в vercel не работает почему-то, говорит ключи нужны, куда мне .env.local выгружать?
- оке тема, а можешь теперь телеграм бота подключить, чтобы когда заказ возникает на 50000 тенге или больше уведомление приходило?
- тут мне нужно telegram id указывать вручную, а я хочу чтоб когда ты /start пишешь тебя в базу добавляло и всем кто там есть отправлялись уведомления
- как мне сделать так, чтоб когда в базе новый заказ на 50000т и больше сообщение отправлялось само?
(тут он мне объяснил про вебхуки и что надо новые секреты создавать и в верселе и в supabase и там в header запросе указывать webhook secret ну и пошагово что к чему и как делать)
- почему Telegram сообщение не пришло, я если что вручную создал Supabase row (оказывается Webhook не триггерится, там только по ссылке или запросу)
- всё тема всё работает, можешь теперь всё в один pipeline завернуть, чтобы можно было в нашем dashboard сразу выгружать json файл?
- можешь сделать так, чтобы можно было добавлять новый заказ прям на dashboard без выгрузки? хочу проеерить тг уведомление
- кайф, а теперь можешь проверить безопасность, вдруг я там где-то что-то не так сделал? 
- я хочу вау эффекта и добавить `/report`, чтобы объяснить ход работы и решения смотря на наш диалог и не отдельным документом, а прямо внутри проекта, и использовать reactflow тип тыкаешь по ноде и там более подробно чтоб было о проблеме, а ноды сами являются поверхностным описанием проблемы
- можешь в репорт странице snap-like behavior сделать как в лендингах бывает же, а то щас визуальный перегруз и непонятно куда смотреть
- можешь пожалуйста привести report и dashboard к одному визуалу и сделать светлую и тёмную темы, а то сейчас mismatch
- напиши пожалуйста readme как пользоваться приложением и походу надо добавить ещё тут report, потому что в тз так написано, вот тз: {текст тз тут}

### где я застрял

самые заметные проблемы были такие:

1. я сначала хотел кинуть док апишки в промпт, но там очень много строк и кредитов бы много съело и я потом подумал он же должен знать про retailcrm, ну так и случилось
2. сначала казалось, что телеграм бот должен реагировать просто на новый заказ в базе, но оказывается деле важен именно вызов webhook.
3. с dashboard возился дольше обычного, а то UI какой-то не очень был, решил через Google Stitch прогнать и уже норм стало
4. переживал по поводу безопасности, ключи, роли в Vercel, всё ли правильно делаю и всё такое, но вроде всё как надо сделал, попросил security review у claude через skills встроенный и вроде хорошо 
5. ну наверно ещё с апишкой телеграма разбирался и тонкостями Supabase и Vercel, ну ChatGPT быстро объяснил поэтому вот как-то так
6. дальше уже полировка была: тут добавь это, сделай репорт таким, сделай то-то
